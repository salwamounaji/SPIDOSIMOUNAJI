/*! Built with http://stenciljs.com */
import { h } from '../stencilrouter.core.js';

import { b as ActiveRouter } from './chunk-30abd2e3.js';

class RouteTitle {
    constructor() {
        this.titleSuffix = '';
        this.pageTitle = '';
    }
    updateDocumentTitle() {
        document.title = `${this.pageTitle}${this.titleSuffix || ''}`;
    }
    componentWillLoad() {
        this.updateDocumentTitle();
    }
    static get is() { return "stencil-route-title"; }
    static get properties() { return {
        "el": {
            "elementRef": true
        },
        "pageTitle": {
            "type": String,
            "attr": "page-title",
            "watchCallbacks": ["updateDocumentTitle"]
        },
        "titleSuffix": {
            "type": String,
            "attr": "title-suffix"
        }
    }; }
}
ActiveRouter.injectProps(RouteTitle, [
    'titleSuffix',
]);

export { RouteTitle as StencilRouteTitle };
